set_from_resource $bg i3wm.color0 #f0f0f0
set_from_resource $fg i3wm.color15 #f0f0f0
set_from_resource $bg_a i3wm.color3 #a0f0f0
set_from_resource $fg_a i3wm.color13 #a0f0f0

set $script_path /my-tools/dotfiles/bash/thinkpad

set $tab_ide  "1: ide"
set $tab_vim  "2: vim"

set $tab_www  "6: www"
set $tab_default  "7: default"
set $tab_mutt  "8: mutt"
set $tab_monitor  "9: monitor"
set $tab_chats "10: messenger"

set $mod Mod4

gaps inner 6
gaps outer 6
# smart_gaps no

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal with tma
bindsym $mod+Return exec termite

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec rofi -show combi

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# next/prev workspace
bindsym $mod+Ctrl+h workspace prev
bindsym $mod+Ctrl+l workspace next

# split in horizontal orientation, PageDown
bindsym $mod+Next split h

# split in vertical orientation, PageUp
bindsym $mod+Prior split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+Shift+s layout stacking
bindsym $mod+Shift+w layout tabbed
bindsym $mod+Shift+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace number $tab_ide
bindsym $mod+2 workspace number $tab_vim
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5

bindsym $mod+6 workspace number $tab_www
bindsym $mod+7 workspace number $tab_default
bindsym $mod+8 workspace number $tab_mutt
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number $tab_chats

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $tab_ide
bindsym $mod+Shift+2 move container to workspace number $tab_vim
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5

bindsym $mod+Shift+6 move container to workspace number $tab_www
bindsym $mod+Shift+7 move container to workspace number $tab_default
bindsym $mod+Shift+8 move container to workspace number $tab_mutt
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number $tab_chats

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt

        bindsym h resize shrink width 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# choose xrandr displaymode
set $mode_display Display mode (d) dual, (m) monitor, (l) laptop
mode "$mode_display" {
    bindsym d exec --no-startup-id exec $script_path/display.sh dual, mode "default"
    bindsym m exec --no-startup-id exec $script_path/display.sh monitor, mode "default"
    bindsym l exec --no-startup-id exec $script_path/display.sh laptop, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+z mode "$mode_display"

# shutdown, reboot, lock script
set $mode_system System (l) lock, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id exec $script_path/i3exit.sh lock, mode "default"
    bindsym s exec --no-startup-id exec $script_path/i3exit.sh suspend, mode "default"
    bindsym r exec --no-startup-id exec $script_path/i3exit.sh reboot, mode "default"
    bindsym h exec --no-startup-id exec $script_path/i3exit.sh hibernate, mode "default"
    bindsym q exec --no-startup-id $script_path/i3exit.sh shutdown, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Escape mode "$mode_system"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

exec_always --no-startup-id /my-tools/dotfiles/polybar/launch-polybar.sh

font pango: Monospace 10

# class                 border  backgr. text indicator child_border
client.focused          $bg     $bg     $fg  $bg       $bg
client.focused_inactive $bg     $bg     $fg  $bg       $bg
client.unfocused        $bg     $bg     $fg  $bg       $bg
client.urgent           $bg     $bg     $fg  $bg       $bg
client.placeholder      $bg     $bg     $fg  $bg       $bg
client.background       $bg

# hide window title
new_window pixel 2

hide_edge_borders both

# go back&forth between workspaces
workspace_auto_back_and_forth yes

# Make the currently focused window a scratchpad
bindsym $mod+Shift+plus move scratchpad

# Show the sup-mail scratchpad window, if any.
bindsym $mod+plus scratchpad show

# SCREENSHOTS
set $FN $HOME/Pictures/screenshots/%Y-%m-%d__%H-%M-%S.png
set $mode_screenshot Screenshot mode (d) desktop (w) window selection
mode "$mode_screenshot" {

    bindsym d exec --no-startup-id exec scrot $FN && notify-send "Screenshot taken", mode "default"
    bindsym --release w exec --no-startup-id scrot -s $FN && notify-send "png screenshot of window taken", mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym Print mode "$mode_screenshot"


# SETTINGS
set $mode_settings Settings mode (r) reload i3 config (a) restart i3 (p) polybar (k) keyboard backlight (t) theme
mode "$mode_settings" {

    # reload i3wm settings
    bindsym r reload

    # restart i3wm
    bindsym a restart

    # restart polybar
    bindsym p exec /home/igor/workspace/dotfiles/polybar/launch-polybar.sh

    # keyboard backlight
    bindsym k exec $script_path/change_kgb_backlight.sh

    # set random theme
    bindsym t exec systemctl start set-random-theme --user

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+r mode "$mode_settings"

# open mail workspace
bindsym $mod+m exec /my-tools/dotfiles/i3/python-scripts/i3-open-workspace-by-name.py mail 

# workspace dmenu
bindsym $mod+w exec /my-tools/dotfiles/i3/python-scripts/workspace-dmenu.py

# dmenu with workspaces
bindsym $mod+Home exec /my-tools/dotfiles/i3/python-scripts/i3-open-workspace-by-name.py REST-CLIENT

# lock screen
bindsym $mod+o exec i3lock -i ~/Pictures/locked.png -t

# skype
bindsym $mod+s exec /my-tools/dotfiles/i3/python-scripts/focus-skype.py

# open cmus
bindsym $mod+c exec /my-tools/dotfiles/i3/python-scripts/i3-open-workspace-by-name.py music 

# cmus pause player
bindsym $mod+p exec cmus-remote --pause

# brightness
bindsym XF86MonBrightnessDown exec $script_path/change_backlight.sh decrement
bindsym XF86MonBrightnessUp exec $script_path/change_backlight.sh increment

# sound
bindsym XF86AudioLowerVolume exec amixer set Master 5%-
bindsym XF86AudioRaiseVolume exec amixer set Master 5%+
bindsym XF86AudioMute exec $script_path/volume_toggle.sh

# assign window to workspace
assign [class="google-chrome"] $tab_www
assign [class="qutebrowser"] $tab_www
assign [class="google-chrome"] $tab_www
assign [class="Thunderbird"] $tab_chats
assign [class="Skype"] $tab_chats
assign [class="Conky"] $tab_monitor
assign [title="PERFOMANCE"] PERFOMANCE

# STARTUP 

# set wallpaper
exec --no-startup-id feh --bg-scale ~/Pictures/wallpapers/wallpaper.jpg

# applications
exec qutebrowser
exec setxkbmap "us,ru" ",winkeys" "grp:caps_toggle"

# fix two monitors mode (what is left and right)
exec "xrandr --output VGA-1 --auto --left-of VGA-1"

# set colors
exec "sleep 2 && ~/.config/virtual-envs/main/bin/wal -i ~/Pictures/wallpapers/active.jpg && i3-msg restart"

# network manager applet
exec --no-startup-id nm-applet --sm-disable

# bluetooth manager applet
exec --no-startup-id blueman-applet

# volume control
# exec --no-startup-id volwheel */

# conky
exec --no-startup-id conky -c /my-tools/home-infostructure/ansible/playbooks/home/dot-files/conky-work.conf
exec --no-startup-id conky -c /my-tools/dotfiles/conky/conky.conf

# compton
exec --no-startup-id compton

# disable bluetooth
exec --no-startup-id "sleep 3 && rfkill block bluetooth"
