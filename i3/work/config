set $tab_www  "1: www"
set $tab_ide  "2: ide"
set $tab_mom  "3: mom"
set $tab_default  "4: default"
set $tab_vim  "5: vim"
set $tab_logs "9: logs"
set $tab_misc "10: misc"
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal with tma
bindsym $mod+Return exec gnome-terminal --window-with-profile="profile-with-init-tma-shell-command"
# start a terminal with no tma
bindsym $mod+Shift+Return exec gnome-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
bindsym $mod+Shift+d exec /my-tools/home-infostructure/python-scripts/i3/i3-workspaces-menu.py

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# next/prev workspace
bindsym $mod+Ctrl+h workspace prev
bindsym $mod+Ctrl+l workspace next


# split in horizontal orientation
bindsym $mod+Alt+h split h

# split in vertical orientation
bindsym $mod+Alt+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace number $tab_www
bindsym $mod+2 workspace number $tab_ide
bindsym $mod+3 workspace number $tab_mom
bindsym $mod+4 workspace number $tab_default
bindsym $mod+5 workspace number $tab_vim
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number $tab_logs
bindsym $mod+0 workspace number $tab_misc

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $tab_www
bindsym $mod+Shift+2 move container to workspace number $tab_ide
bindsym $mod+Shift+3 move container to workspace number $tab_mom
bindsym $mod+Shift+4 move container to workspace number $tab_default
bindsym $mod+Shift+5 move container to workspace number $tab_vim
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number $tab_logs
bindsym $mod+Shift+0 move container to workspace number $tab_misc

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
   
    colors {
	# Whole color settings
	background #000c00
	statusline #ffffff
	separator  #666666

	# Type             border  background font
	focused_workspace  #008fff #007fff #ffffff
	active_workspace   #333333 #5f676a #ffffff
	inactive_workspace #333333 #222222 #888888
	urgent_workspace   #aa0000 #990000 #ffffff
    }
    font pango: Monospace 9
    status_command /home/igor/.virtualenvs/main_env/bin/py3status 
    #status_command i3status 
}

font pango: Monospace 9

bindsym $mod+Ctrl+p exec ~/.bin/mom_client_vip_production.sh
bindsym $mod+Ctrl+s exec ~/.bin/mom_client_vip_staging.sh
bindsym $mod+Ctrl+d exec ~/.bin/mom_client_vip_development2.sh


# Make the currently focused window a scratchpad
bindsym $mod+Shift+plus move scratchpad

# Show the sup-mail scratchpad window, if any.
bindsym $mod+plus scratchpad show

# screenshot
bindsym --release $mod+x exec --no-startup-id import ~/Pictures/screenshots/file_$(date +%Y_%m_%d___%H_%M_%S).png

# open mail workspace
bindsym $mod+Ctrl+m exec /my-tools/dotfiles/i3/python-scripts/i3-open-workspace-by-name.py mail 

# open rest-rest workspace
bindsym $mod+Ctrl+r exec /my-tools/dotfiles/i3/python-scripts/i3-open-workspace-by-name.py rest-test 

bindsym $mod+Ctrl+1 exec /my-tools/dotfiles/i3/python-scripts/focus-skype.py
bindsym $mod+Ctrl+2 exec /my-tools/dotfiles/i3/python-scripts/focus-thunderbird.py
bindsym $mod+Ctrl+3 exec /my-tools/dotfiles/i3/python-scripts/focus-timetracker.py
bindsym $mod+Ctrl+5 exec /my-tools/dotfiles/i3/python-scripts/open-logging.py
bindsym $mod+Ctrl+0 exec i3lock -i ~/Pictures/locked.png -t

# cmus pause player
bindsym $mod+Shift+Insert exec cmus-remote --pause

# kill local servers
bindsym $mod+Shift+End exec ~/.bin/kill_local_servers.sh

# assign window to workspace
assign [class="google-chrome"] $tab_www
assign [class="jetbrains-idea-ce"] $tab_ide
assign [class="org-java-plugin-boot-Boot"] $tab_mom
assign [class="jetbrains-pycharm-ce"] 8
assign [class="Amarok"] $tab_misc
assign [class="Thunderbird"] $tab_misc
assign [class="Skype"] $tab_misc
assign [class="Wine"] $tab_misc
for_window [class="org-java-plugin-boot-Boot"] floating disable
for_window [class="Wine"] floating disable

workspace 0 output HDMI1
#workspace 1 output VGA1
workspace 2 output HDMI1
workspace 3 output HDMI1
workspace 8 output HDMI1
workspace 9 output HDMI1

# STARTUP 

# set wallpaper
exec --no-startup-id feh --bg-scale ~/Pictures/wallpapers/wallpaper.jpg

# applications
exec skype 
exec idea 
exec "google-chrome"
exec setxkbmap "us,ru" ",winkeys" "grp:caps_toggle"
exec timetracker.sh 
exec dk start 0c1597ecaaa1 #opencms postgres
exec dk start 24481de5aa3c #bof postgres
exec dk start 48ef1c5f6415 #apache web server
exec "xrandr --output VGA1 --auto --left-of HDMI1"

# exec workspace $tab_misc layout tabbed
