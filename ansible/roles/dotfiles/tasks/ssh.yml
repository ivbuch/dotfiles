- name: create directory
  file:
    path: "{{ ansible_env.HOME }}/.ssh"
    state: directory
  tags:
    - ssh

- name: create link to ~/.ssh/config
  file:
    src: "{{ homeinfostructure_path }}/dotfiles/ssh-config"
    dest: "{{ ansible_env.HOME }}/.ssh/config"
    state: link
  tags:
    - ssh
